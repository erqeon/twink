Important: i closed Issues and Pull Requests not because i don't need help—quite the opposite, 
i do—but because i don't need millions of lines of code generated by bots (ai).

Where (to send): erqeon@gmail.com

Format: You can either simply describe the bug in text or submit the actual change files generated using the 'git format-patch' command.

Reward: When i accept patches from developers, i add their names to the CREDITS file.